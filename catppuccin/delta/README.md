## Catppuccin theme in delta pager

Must setup catppuccin on `bat` first

Add this in your ~/.gitconfig file

```
[include]
  path = ~/dotfiles/catppuccin/delta/catppuccin.gitconfig
[delta]
  features = catppuccin-macchiato
```
